package designPattern.decoratorDesignPattern;


/**
 * When is-A and has-A relationship both exists we use decorator design pattern.
 * It is used for example we have base pizza then we have types of it like margiretta, peppy paneer. These
 * will be the child classes of base class Pizza. Now we can have again toppings like extra cheese...
 * So we will make another class named toppings inheriting class Pizza and will also have a instance variable
 * of Class Pizza.
 */
public class DecoratorDesignPattern {

}
